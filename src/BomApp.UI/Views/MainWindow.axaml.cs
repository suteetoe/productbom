using Avalonia.Controls;

namespace BomApp.UI.Views;

/// <summary>Main shell window — code-behind is intentionally minimal.</summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
