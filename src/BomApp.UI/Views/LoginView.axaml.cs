using Avalonia.Controls;

namespace BomApp.UI.Views;

/// <summary>Login window — code-behind is intentionally minimal.</summary>
public partial class LoginView : Window
{
    public LoginView()
    {
        InitializeComponent();
    }
}
